server:
  port: 8080

spring:
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server-addr}
        file-extension: yaml
      discovery:
        server-addr: ${nacos.server-addr}

nacos:
  server-addr: 59.110.236.186:8848
