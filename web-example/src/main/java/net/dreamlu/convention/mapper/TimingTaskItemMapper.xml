<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.dreamlu.convention.mapper.TimingTaskItemMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="net.dreamlu.convention.model.TimingTaskItem">
        <id column="id" property="id"/>
        <result column="createTime" property="createTime"/>
        <result column="hasComplete" property="hasComplete"/>
        <result column="msg" property="msg"/>
        <result column="triggerTime" property="triggerTime"/>
        <result column="type" property="type"/>
        <result column="updateTime" property="updateTime"/>
        <result column="status" property="status"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, createTime, hasComplete, msg, triggerTime, type, updateTime, status
    </sql>

</mapper>
